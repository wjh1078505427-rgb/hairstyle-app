# AI换发型 v2 — QQ邮箱登录 + 积分制 设计文档

> 状态：已确认 · 2026-06-10

---

## 1. 目标

| 功能 | 现状 | v2 |
|------|------|-----|
| 用户身份 | 纯文本用户名（无验证） | QQ邮箱验证码登录 |
| 免费额度 | 5次/天/设备，易绕过 | **1次/天/设备**（未登录）；登录后送 888 积分 |
| 付费 | ¥0.80/次 | **积分制**：¥9.9/月 = 10,000 积分，888 积分/次 |
| 收款 | 手动充值码 | 微信赞赏码 → 截图 → 后台手动充值（以后换 xorpay 自动） |
| 数据存储 | 本地 JSON | 本地 JSON（不变） |

---

## 2. 用户流程

```
用户打开页面
  ├─ 未登录 → 显示 "今日免费 1/1 次"
  │     ├─ 用掉1次 → 弹出 "注册送 888 积分继续"
  │     └─ 点注册 → QQ邮箱验证码登录
  │
  ├─ 已登录 → 显示积分余额
  │     ├─ 积分够（≥888）→ 扣积分生成
  │     ├─ 积分不够 → 引导充值 ¥9.9/月
  │     └─ 充值 → 微信扫码赞赏 → 截图发给客服 → 后台手动充值
  │
  └─ 未登录 + 已用完 → 必须登录才能继续
```

---

## 3. 定价模型（积分制）

| 项目 | 数值 |
|------|------|
| 免费注册送 | **888 积分**（= 1次） |
| 包月 | **¥9.9 = 10,000 积分**（≈ 11次，合 ¥0.88/次） |
| 每次消耗 | **888 积分** |
| API成本 | ¥0.02/次 |
| 毛利 | ¥9.9 - 11×¥0.02 ≈ **¥9.68** |

> 用积分让价格感弱化，用户心理上觉得 888 积分比 ¥0.80 轻。

---

## 4. 数据模型

### 4.1 用户表 `users`

```json
{
  "user_123456789@qq.com": {
    "email": "123456789@qq.com",
    "points": 888,              // 积分（替代 balance）
    "total_used": 0,
    "total_spent_points": 0,
    "registered": "2026-06-10T12:00:00",
    "device_ids": ["dev_abc123"],
    "recharge_log": [
      {"points": 10000, "amount": 9.9, "time": "...", "note": "赞赏码充值"}
    ]
  }
}
```

### 4.2 设备表 `devices`

```json
{
  "dev_abc123": {
    "ip_hash": "sha256...",
    "ua_hash": "sha256...",
    "created": "2026-06-10",
    "daily_usage": {"2026-06-10": 1},   // 未登录免费次数
    "total_usage": 5,
    "linked_user": "user_xxx@qq.com"
  }
}
```

- `daily_usage` 只记录**未登录**的免费使用
- 登录后走积分扣费，不计入 daily_usage

### 4.3 验证码表 `verification_codes`

```json
{
  "codes": {
    "123456789@qq.com": {
      "code": "483921",
      "expires_at": "2026-06-10T12:05:00",
      "attempts": 0
    }
  }
}
```

- 6位数字，5分钟有效，最多试3次
- 60秒内同邮箱不能重发
- 同IP每天最多5次，每小时最多3次

---

## 5. QQ邮箱 SMTP 配置

### 5.1 你需要做的（5分钟）

1. 登录 QQ 邮箱 → 设置 → 账户
2. 找到 "POP3/SMTP 服务" → **开启**
3. 生成**授权码**（16位，不是QQ密码）
4. 把授权码给我，我配到 `.env`

### 5.2 SMTP 参数

| 参数 | 值 |
|------|-----|
| 服务器 | `smtp.qq.com` |
| 端口 | 465 (SSL) |
| 每日限额 | ~500封 |
| 费用 | **¥0** |

### 5.3 邮件模板

```
主题：AI换发型 - 登录验证码

你的验证码是：483921
5分钟内有效，请勿泄露。
```

---

## 6. 收款方案：微信赞赏码（手动）

```
用户点击充值 → 弹出你的微信赞赏码
  → 用户扫码付 ¥9.9
  → 截图发你微信
  → 你在后台手动给用户加 10,000 积分
```

| 优点 | 缺点 |
|------|------|
| ¥0成本，¥0手续费 | 需要人工操作 |
| 不用企业资质 | 适合 < 50单/天 |
| 5分钟搞定 | 做大了换 xorpay |

> xorpay（以后再换）：个人可接入微信/支付宝支付，0.38%手续费，回调自动充值。

---

## 7. 设备指纹策略

```
IP + User-Agent + Accept-Language + Accept-Encoding
  → SHA256 哈希 → 设备 ID
```

| 绕过方式 | 难度 |
|------|------|
| 刷新页面 | ❌ 不能 |
| 隐私模式 | ✅ 能（需要主动开） |
| 换浏览器 | ✅ 能（需要下载） |
| 同IP同浏览器 | ❌ 不能 |

> 对 ¥9.9 产品够了——没人为了省 ¥0.88 去折腾。

---

## 8. 文件结构

```
hairstyle-app/
├── app.py          # 主界面（改动：QQ邮箱登录UI + 积分显示）
├── auth.py         # 新增：验证码生成/发送/验证
├── smtp.py         # 新增：QQ邮箱 SMTP 发送
├── db.py           # 改动：积分系统 + 验证码 + 限流
├── config.py       # 改动：积分定价常量
├── api_client.py   # 不变
├── .env            # 新增 QQ_EMAIL, QQ_SMTP_PASSWORD
└── data/db.json    # 不变，结构自动升级
```

---

## 9. 实现计划

| 步骤 | 内容 | 状态 |
|------|------|------|
| 1 | 确认设计文档 | ✅ 已确认 |
| 2 | 你提供 QQ邮箱 + SMTP 授权码 | 🔜 等你 |
| 3 | 写 `smtp.py` + `auth.py` | 拿到授权码后 |
| 4 | 改 `db.py`（积分+验证码+限流） | 同上 |
| 5 | 改 `config.py`（积分常量） | 同上 |
| 6 | 改 `app.py`（登录UI+积分显示） | 同上 |
| 7 | 本地测试 | |
| 8 | 部署上线 | |
